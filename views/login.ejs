<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
  </head>
  <body class="flex min-h-screen items-center justify-center bg-gray-100">
    <form
      action="/login"
      method="post"
      class="w-96 rounded bg-white p-8 shadow-md"
    >
      <h2 class="mb-4 text-2xl font-bold">Login</h2>

      <input
        type="email"
        placeholder="Email"
        name="email"
        required
        class="w-full block p-2 mb-4 border rounded"
      />
      <input
        type="password"
        placeholder="Password"
        name="password"
        required
        minlength="6"
        class="w-full block p-2 mb-4 border rounded"
      />
      <% if (typeof error !== "undefined") { %>
      <p class="mb-4 text-red-500 text-center"><%= error %></p>
      <% } %>
      <button
        class="w-full px-4 font-semibold py-2 bg-green-500 text-white rounded"
      >
        Login
      </button>
      <div class="mt-4 flex justify-center space-x-1">
        <h4 class="text-gray-700">Don't have an account?</h4>
        <a href="/register" class="text-blue-600">Register</a>
      </div>
    </form>
  </body>
</html>
